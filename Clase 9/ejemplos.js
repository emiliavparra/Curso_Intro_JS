// Expresion : devuelve un valor. Por ej:
5
3 + 4
edad < 18

// Declaracion: define o hace algo. No siempre devuelve un valor
let suma = c + b
let a = 10

// Sentencias: cada linea completa
console.log()