// Ejemplo

// Aqui 'nombre' es el parametro y va a ser el valor que se pasa a la funcion
function saludar(nombre) {
    console.log('Hola, ' + nombre + '!')
}

// Invocacion de la funcion
saludar('Emi')

// Otro ejemplo
function sumar(a, b) {
    return a + b;
}

sumar(3, 4)